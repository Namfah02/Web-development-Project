<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mobile Hour - Products</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <main>
        <%- include("partials/header.ejs") %>
            <h1>Products</h1>
            <form action="product_item" method="get" class="search-product">
                <section class="browse-product">
                    <section>
                        <select name="filterBrand" onChange="window.location.href=this.value">
                            <option disabled selected value>Select Brand</option>
                            <option value="product_item?brand=All">All</option>
                            <option value="product_item?brand=Apple">Apple</option>
                            <option value="product_item?brand=Samsung">Samsung</option>
                            <option value="product_item?brand=Tecno">Tecno</option>
                        </select>
                        <select name="filterPrice" onChange="window.location.href=this.value">
                            <option disabled selected value>Select Price </option>
                            <option value="product_item?price=5000">All</option>
                            <option value="product_item?price=500">500 or less</option>
                            <option value="product_item?price=1000">1000 or less</option>
                            <option value="product_item?price=2000">2000 or less</option>
                        </select>
                    </section>

                    <section>
                        <input type="text" name="search_product" id="search-product" placeholder="Search"
                            class="search-form" />
                        <input type="submit" value="Search" class="search-button" />
                    </section>
                </section>
                <section class="product-list">
                    <% for (let product of products) { %>
                        <article class="product-card">
                            <figure><img src="images/mobilephone.jpg" alt="Mobile Phone image" />
                                <h2>
                                    <%= product.name %>
                                </h2>
                                <figcaption>$<%= product.price %>
                                </figcaption>
                                <p>Stock: <% if (product.stock> 0) { %><%= product.stock %>
                                            <% } else { %>Out of stock <% } %>
                                </p>
                                <a href="product_details?id=<%= product.id %>" class="buy-button">Buy now</a>
                            </figure>
                        </article>
                        <% } %>
                </section>
            </form>
            <%- include("partials/footer.ejs") %>
    </main>
</body>

</html>